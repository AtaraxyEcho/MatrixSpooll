# MatrixSpooll local PostgreSQL development environment.
# Copy this file to .env before starting Docker Compose.

# Development-only password. Use a strong secret outside local development.
POSTGRES_PASSWORD=matrixspooll_dev
POSTGRES_PORT=5432
TZ=Asia/Shanghai
