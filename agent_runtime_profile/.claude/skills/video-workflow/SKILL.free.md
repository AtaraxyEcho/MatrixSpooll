---
name: video-workflow
description: 自由创作项目的请求路由。用户要求生成图片、视频、音频或编辑现有内容时使用。
---

# 自由创作项目路由
<!-- mode: free -->

自由创作项目没有固定工作流计划。不要调用 `get_workflow_plan`、剧本生成、分镜生成、宫格生成或分集视频工具。

将用户请求归一为自由创作请求，明确 `output_type`、原始 prompt、参考素材角色、比例和父版本。图片、视频和编辑请求分别交给对应的自由创作工具；任务完成后返回产物版本和失败原因。
